# Amplify Fusion - Unstructured to Structured Classifer

Users LlamaParse to convert pdf files to text and then Groq LLM to classify and convert to JSON. Results are stored in a Postgres DB for complex queries of structured data.